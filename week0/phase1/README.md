#phase 1 --SQL to PySpark

learn basic sql quries and their equivalent PySpark DataFrame Opertations

## objective 
-Dataframe as a table 
-show()
-select()
-filter()
-groupBy()

## SQL vs PySpark Mapping
SQL             PySpark
--
SELECT         select()
WHERE          filter()
GROUP BY       groupBy()
COUNT          count()

## Conclusion
This Phase bulid the foundation for working with pyspark and prepare for advanced topics like joins and aggregations

